spring:
  application:
    name: vsph-multimedia-service
  profiles:
    group:
      dev:
        - 'microservice-dev'
        - 'kafka'
        - 'kafka-dev'
        - 'eureka-config'
      docker:
        - 'microservice-docker'
        - 'kafka'
        - 'kafka-docker'
        - 'eureka-config'

visphere:
  s3:
    region: eu-central-1
    path-style-access-enabled: true