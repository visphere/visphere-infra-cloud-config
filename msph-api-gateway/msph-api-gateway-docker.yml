server:
  port: 8080

moonsphere:
  allowedOrigins:
    - http://localhost:${MSPH_DOCKER_LANDING_PORT:5052}
    - http://localhost:${MSPH_DOCKER_CLIENT_PORT:5053}

eureka:
  client:
    service-url:
      defaultZone: http://${V_EUREKA_USERNAME}:${V_EUREKA_PASSWORD}@msph-discovery-server:8761/eureka