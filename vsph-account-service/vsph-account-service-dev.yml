spring:
  datasource:
    url: jdbc:postgresql://localhost:${VSPH_DEV_POSTGRESQL_PORT:7500}/vsph_account

visphere:
  openapi:
    service:
      title: Account service
      version: 1.0.0
      url: http://localhost:${VSPH_DEV_API_GATEWAY_PORT:6055}
  jwt:
    secret-key: ${VSPH_JWT_SECRET_KEY}
    issuer: ${VSPH_JWT_ISSUER}
    audience: ${VSPH_JWT_AUDIENCE}

springdoc:
  api-docs:
    path: /vsph-account-service/v3/api-docs
  swagger-ui:
    path: /vsph-account-service/swagger-ui.html