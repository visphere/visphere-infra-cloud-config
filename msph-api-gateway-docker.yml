moonsphere:
  clients:
    landingPage: http://localhost:${MSPH_DEV_LANDING_PORT:5052}
    webClient: http://localhost:${MSPH_DEV_CLIENT_PORT:5053}

server:
  port: 8080

eureka:
  client:
    service-url:
      defaultZone: http://${V_EUREKA_USERNAME}:${V_EUREKA_PASSWORD}@msph-discovery-server:8761/eureka