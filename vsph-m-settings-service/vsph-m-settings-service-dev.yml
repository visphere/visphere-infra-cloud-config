spring:
  datasource:
    url: jdbc:postgresql://localhost:${VSPH_DEV_POSTGRESQL_PORT:7500}/vsph_settings

visphere:
  openapi:
    service:
      title: Settings service
      version: 1.0.0
      url: http://localhost:${VSPH_DEV_API_GATEWAY_PORT:6055}

springdoc:
  api-docs:
    path: /vsph-m-settings-service/v3/api-docs
  swagger-ui:
    path: /vsph-m-settings-service/swagger-ui.html