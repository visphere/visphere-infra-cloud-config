spring:
  application:
    name: msph-discovery-server
  banner:
    location: classpath:spring/banner.txt

eureka:
  instance:
    hostname: machine_ip
  client:
    register-with-eureka: false
    fetch-registry: false
    healthcheck:
      enabled: true